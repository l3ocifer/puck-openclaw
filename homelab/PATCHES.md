# Local patches vs upstream openclaw/openclaw

Track non-additive changes (anything outside `homelab/`).

## Active patches

_(none today — Puck ships pure-vanilla OpenClaw, customized only via
configs in `homelab/config/`. Puck's own work — finished pieces,
drafts, methodology notes — lives in `puck-graph/pages/`, not in
source code.)_
